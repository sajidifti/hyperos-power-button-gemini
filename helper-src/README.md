# Gemini backend guard

This no-icon helper has no running service. Android starts its receiver only when the Google app is replaced or updated. The receiver restores Google's voice-interaction component, which HyperOS may clear during an in-place Google app update.
